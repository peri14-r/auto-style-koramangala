import fs from 'node:fs';import{createRequire}from'node:module';const require=createRequire(import.meta.url);const{chromium}=require('C:/Users/Periyasamy/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',args:['--enable-unsafe-swiftshader']});
for(const [name,width,height]of[['desktop',1440,1000],['mobile',390,844]]){const ctx=await browser.newContext({viewport:{width,height},deviceScaleFactor:1});await ctx.addInitScript(()=>{Element.prototype.requestPointerLock=()=>{};Element.prototype.setPointerCapture=()=>{};Element.prototype.releasePointerCapture=()=>{};});const page=await ctx.newPage();const errors=new Set();page.on('pageerror',e=>errors.add(e.message));page.on('console',m=>{if(m.type()==='error'&&!m.text().includes('404'))errors.add(m.text())});await page.goto('http://localhost:5190/',{waitUntil:'domcontentloaded'});await page.waitForSelector('.hero-scene.ready',{timeout:30000});await page.waitForTimeout(1500);
for(const mode of ['studio']){const pos=await page.locator(mode==='hero'?'#top':'#studio').evaluate(el=>el.getBoundingClientRect().top+scrollY);await page.evaluate(y=>scrollTo({top:y,behavior:'instant'}),mode==='hero'?0:pos);await page.waitForSelector(`.${mode}-scene.ready`);await page.waitForTimeout(1500);const data=await page.locator(`.${mode}-scene`).evaluate(el=>{el.dispatchEvent(new Event('scene:capture'));return el.dataset.poster});if(data)fs.writeFileSync(`scrollcraft/builds/showroom/${mode}-${name}.png`,Buffer.from(data.split(',')[1],'base64'));await page.screenshot({path:`scrollcraft/builds/showroom/refined-${mode}-${name}.png`});}
console.log(name,'errors',Array.from(errors),'overflow',await page.evaluate(()=>document.documentElement.scrollWidth-innerWidth));await ctx.close();}await browser.close();
